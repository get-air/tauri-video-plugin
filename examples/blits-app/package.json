{
  "name": "air-blits-video-example",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "prepare:adapter": "node scripts/prepare-linked-adapter.mjs",
    "dev": "npm run prepare:adapter && vite --force",
    "typecheck": "tsc --noEmit",
    "build": "npm run prepare:adapter && npm run typecheck && vite build",
    "tauri": "tauri"
  },
  "allowScripts": { "esbuild": true },
  "dependencies": {
    "@get-air/video": "git+https://github.com/get-air/video.git#ff51658a5fc2de29fc21a76d0f1661486caa5d98",
    "@get-air/video-tauri": "file:../..",
    "@lightningjs/blits": "^2.8.7",
    "@tauri-apps/api": "^2.0.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "typescript": "^5.9.3",
    "vite": "^7.0.0"
  }
}
